# Seguimiento de la auditoría del 07-09-2026

Snapshot auditado: `86001d2`. Contraste contra el binario real (07-09, sesión Claude Code): `repro-2026-09-07.py`
y `repro2-2026-09-07.py` en esta carpeta, ejecutados sobre `af6ee5e` (mismo código que el snapshot).
Resultado: 17 comprobaciones ejecutadas, 17 reproducidas; cubren RMP-001 (a, a', b, c, d, e), 002 (2),
003 (a, b), 004, 005, 007, 008, 009, 010 (2), 011, 013, 014, 015; RMP-016 confirmado por API
(`main` `protected: false`, 0 rulesets). No reproducidos entonces: 006, 012, 017, 018, 019.

Baterías de partida sobre `8c8c6e9` (07-09): `rompelo.tests` 113/113 · `rompelo.observe-tests` 75/75.
Son cifras históricas; cada lote anota las suyas.

Rama de trabajo: `mejoras-auditoria-2026-09-07` en el worktree `~/rompelo-dev`. `~/rompelo` (el binario que
ejecutan los hooks activos de todas las sesiones) no se toca hasta que José decida.

## Tabla por hallazgo

Formato: estado previo → prueba roja → cambio → prueba verde → regresiones → limitaciones → revisión.

| ID | Estado previo | Prueba roja | Cambio | Prueba verde | Regresiones | Limitaciones | Revisión |
|---|---|---|---|---|---|---|---|
| RMP-004 | `espera_paso` aceptaba stdout vacío sin mirar código ni stderr; helpers del observador tiraban `returncode`; `observe-test` ejecutaba `~/rompelo/bin/rompelo` a mano (el binario en producción, no el del árbol probado) | `repro-2026-09-07.py`: hook falso con rc=1 y traceback → `PASS=1 FAIL=0` | `tests/lib.sh` + `tests/invocar.py`: toda invocación pasa por un runner con plazo que exige rc 0 y stderr vacío y mete marca ⟦INSTRUMENTO ROTO⟧ en la salida; `espera_bloqueo` parsea la salida ENTERA como JSON; binario por defecto = el que está junto a los tests; `resumen` devuelve 1 si hubo roturas aunque nadie mirase la salida; `control-positivo.py` exige `ROTOS=0` | `tests/instrumento-test.sh`: 26/26 (muerto, ausente, sin +x, JSON truncado, contaminado, rc 1 con JSON bueno, stderr con JSON bueno, sin respuesta en 2 s, stdin perdido; y el control negativo: silencio sano y bloqueo sano pasan) | `rompelo.tests` 113/113 ROTOS=0 · `observe-tests` 75/75 ROTOS=0 · `control-positivo.py gate` → 1 (112/1, solo el fallo de scope) | El primer runner usaba un heredoc y perdía el stdin: lo cazó el control con un falso que devuelve lo que recibe, no la revisión. Queda documentado en `invocar.py` | pendiente |
| RMP-001 | `ficheros_cambiados` partía la salida humana de `git diff --name-only` por líneas; `año.py` llegaba como `"a\303\261o.py"`, `huella_arbol` no lo encontraba y lo firmaba como `D`; el modo y el destino del enlace no entraban en la huella; en un repo sin HEAD lo preparado no contaba | `repro2-2026-09-07.py` (5/5) y la sección «huella» de `rompelo-stop-test.sh`: 16 ❌ antes del cambio | `-z --no-renames` + `surrogateescape`; `sujeto()` = D · F:/X:<sha256> · L:<sha del destino> · S:<commit> · T:dir; huella `v2:<16 hex>`; evidencia sin `v2:` = «formato antiguo, vuelve a ejecutar»; repo sin commits = árbol vacío; `git_ok` levanta `GitError` (fail-closed en el hook) | 138/138: Unicode, tab, espacio, dos modificaciones seguidas, +x en limpio y sobre modificado, symlink t2→t3, renombrado (D+A), commit del mismo contenido en silencio, `año.py` dentro de `src/*.py`, git con índice ilegible → bloqueo | observe 75/75 · instrumento 26/26 · mutante de scope → 1 (137/1) · `repro-2026-09-07.py`: 001a-d y 002 pasan a NO reproducido | Ficheros ignorados por `.gitignore` no cuentan (documentado). Un submódulo se firma por su commit, no por su contenido. Renombrado = borrado + alta, no se conserva el vínculo | pendiente |
| RMP-002 | `ref_base` devolvía `HEAD` si la base no existía; la CI propia hacía checkout superficial | `repro-2026-09-07.py` RMP-002/002b; batería h16 | base explícita ausente → `ValueError` con instrucción (fetch o corregir); `sin-head` = árbol vacío explícito; `ci.yml` con `fetch-depth: 0` | h16 en verde: bloqueo «no existe en este repo» | idem | El rango de PR frente a la base de tarea sigue siendo la base del contrato: CI juzga lo que el contrato dice, no lo que GitHub calcula. NO VERIFICADO en Actions hasta el push | pendiente |
| RMP-015 (mínimo) | borrar el único test satisfacía `exige_prueba_en_diff` | h23 rojo | una ruta con `sujeto == "D"` no cuenta como prueba en el diff | h23/h24 verdes | idem | No vincula el requisito al test ejecutado (eso es bloque F) | pendiente |
| RMP-003 | `repos_permitidos` fuera del try → JSONDecodeError sin JSON de decisión; `leer_json_o` → `{}` borraba nivel y perfiles; escrituras directas sin atomicidad ni cerrojo; `permisos.json` corrupto se pisaba | batería «errores propios» (e1, e4, permisos) y «estado concurrente»: 5 ❌ y ROTOS=1 antes del cambio; observe-test «estado corrupto» ❌ contra `~/rompelo` | `leer_json_estricto` (ausente → defecto, corrupto → ValueError con ruta); allowlist ilegible → bloqueo con mensaje y tope de 3; estado corrupto → fail-closed en el gate, silencio + `observe.err` en el observador, fichero intacto; `escribir_json_atomico` (tmp + fsync + replace); `cerrojo` flock alrededor del leer-modificar-escribir del observador, permisos y allowlist; `ROMPELO_RETARDO_ESTADO` solo para ensanchar la carrera en la batería | 160/160 y 77/77; quitando el cerrojo a propósito (mutante `nullcontext`) la prueba de concurrencia cae: faltan 4 de 5 perfiles | instrumento 26/26 · mutante de scope → 1 (159/1) | Los ficheros de **configuración** (`observacion.json`, `riesgo*.json`) siguen leyéndose con tolerancia (`leer_json_o`): un JSON roto ahí vuelve a los valores del código, que son los más estrictos. Interrupción a medio `os.replace`: no simulada; la garantía es la del sistema de ficheros | pendiente |
| RMP-007 | `ejecutar` guardaba `argv` entero; informe y motivo del cruce fallido lo repetían; `ROMPELO_DEBUG_FORMA` guardaba 120 caracteres de texto | batería «privacidad»: canarios en argumentos del check, del cruce, en URL y en salida → 3 ❌ (evidencia, motivo, informe) | evidencia = `programa`, `argv_sha`, `n_args`, código, tiempo, `lineas_salida`, `bytes_salida`, `salida_truncada`, `sha_salida`; informe y motivos dicen programa y nº de argumentos; debug guarda longitud | 4/4 canarios ausentes de evidencia, motivo de bloqueo, informe y salida de `close` | idem | `rompelo cruce` sigue imprimiendo en terminal el comando y las últimas 15 líneas si falla: es transitorio y el agente ya vio esa salida, pero es salida de Bash (chat). `rompelo check` imprime el argv del registro. Un hash no protege un argv corto: por eso el argv no se guarda, ni hasheado se afirma «protegido» | pendiente |
| RMP-010 | `subprocess.run` sin plazo, captura ilimitada, `text=True` → UnicodeDecodeError sin capturar; `OSError` → «FALLÓ con código 127» | batería «runner»: 6 ❌ (plazo 31 s, gate, binario, chorro, ausente, timeout no numérico) | Popen sin shell + hilo lector acotado (4 MiB) + `wait(timeout)` + `killpg` TERM→KILL + `start_new_session`; bytes, no texto; `timeout` por check validado; `instrumento: no_arranco / sin_respuesta` con motivo propio en check y en control positivo; `KeyboardInterrupt` mata el grupo | 7/7: corta en ~1 s, sin hijos vivos, bloqueo «no terminó», `0xff` pasa, 6 MB → `salida_truncada` con tamaño real, ausente → «no pudo ejecutarse», `timeout: "mucho"` rechazado | idem | La cancelación por SIGINT no está en la batería (mata el grupo por código, no verificada). `sha_salida` y `lineas_salida` se calculan sobre los 4 MiB conservados y lo dice `salida_truncada` | pendiente |
| RMP-005 | `registro()` solo caía al `.rompelo/registry.json` del repo si ROMPELO_HOME no tenía ninguno; la plantilla clonaba rompelo (con registro propio) como ROMPELO_HOME | batería «registro del consumidor»: 3 ❌ (repo con global, sin variable, fallback implícito) | `ROMPELO_REGISTRO=home\|repo` explícito; sin registro → error que dice cómo habilitar el del repo; `verify --ci` imprime `registro: …`; plantilla con la variable, `permissions: contents: read`, `ROMPELO_REV` | 5/5, incluida la topología real (HOME con registro global + variable → gana el del consumidor) | 185/185 · 77/77 · 26/26 · mutante → 1 | La plantilla no se ha ejecutado en GitHub Actions desde aquí: NO VERIFICADO en runner real | pendiente |
| RMP-006 | `--ci` acababa en «OK: la tarea puede cerrarse» con checks `solo_local` omitidos y la junta sin cruzar; `--estricto` no bloqueaba los omitidos | batería «resultado de CI»: 8 ❌ | `resultados` por obligación (PASS/FAIL/ERROR/SKIPPED/WAIVED), `completo` en JSON, texto «OK PARCIAL, contrato INCOMPLETO»; `--estricto` bloquea SKIPPED y acepta WAIVED; `excepciones: [{que, motivo, quien}]` validadas (sin motivo/quién → error) | 9/9 | idem | El código de salida sin `--estricto` sigue siendo 0 con SKIPPED (compatibilidad): la distinción va en el texto y el JSON | pendiente |
| RMP-008 | `motivos_bloqueo` hasheaba la copia con `toca_junta=True` forzado por perfil; `close` firmaba el original | batería «contrato efectivo»: ef1–ef4 ❌ | `contrato_efectivo()` único; `info.hash` = contrato escrito; `close` escribe `obligaciones_efectivas` (entra en el hash) | verify tras close en silencio; informe dice «exigida por el perfil»; rebajar a mano las obligaciones invalida el cierre | idem | En local manda el estado del observador; en CI, `obligaciones_efectivas`. Un clon local sin estado exige menos que CI hasta el siguiente `close` (documentado) | pendiente |
| RMP-014 | `cmd_check` salía con «no tiene checks» antes de calcular los de nivel 3 | batería: «nivel3 con checks vacío» ❌ | exigidos = `contrato_efectivo` antes de decidir | ejecuta y cierra; CI sin estado ejecuta el de nivel 3 que dejó el cierre | idem | — | pendiente |
| transversal | tope de 3 → `systemMessage` y nada persistente | batería «tope alcanzado ≠ verificado»: 2 ❌ | `SIN-VERIFICAR.json` con motivos; `status` lo muestra; `close` real lo borra | 3/3 | idem | — | pendiente |
| RMP-009 | `permiso … no` tras `si` no revocaba; nivel 3 guardado como número; todos los `checks_nivel3` exigidos por cualquier permiso; permiso no recordado se heredaba | observe-test «permisos»: 5 ❌ contra `~/rompelo` | `permisos_de` / `nivel_efectivo`; permisos como `{patron, tarea, recordar, fecha, revocado}`; alcance por id de check o general; revocado → check PENDIENTE con motivo propio, resoluble con permiso nuevo o `excepciones`; `nivel bajar` vacía permisos; `obligaciones_efectivas.permisos` | 8/8 (sí, alcance A≠B, no revoca y baja nivel, pendiente, excepción, general exige todos, sin --recordar no se hereda, con --recordar sí) | gate 185/185 · observador 91/91 · instrumento 26/26 · mutante → 1 | Un `nivel: 3` de estado antiguo deja de valer (hay que volver a dar el permiso): cambio de política documentado en observación §12.5 | pendiente |
| RMP-011 | `hookEventName` fijo «PostToolUse» | observe-test: 1 ❌ | `hookEventName = ev["evento"]` | 2/2 (Failure → PostToolUseFailure; PostToolUse → PostToolUse) | idem | NO VERIFICADO en Claude Code real: que el cliente acepte la respuesta con ese nombre (sigue la doc oficial) | pendiente |
| RMP-019 | pendiente de runtime en la auditoría. Medido aquí: 64 eventos `apply_patch` reales con `ficheros` vacío | observe-test «apply_patch»: 3 ❌ contra `~/rompelo` | rutas de `*** Add/Update/Delete File` y `Move to` desde `tool_input.command` (doc oficial), absolutas y reales respecto al `cwd` del evento; el cuerpo del parche no entra en el riesgo | 4/4 en batería + **sesión real** codex-cli 0.153.4 (repo desechable, hooks de proyecto, `--dangerously-bypass-hook-trust`): `apply_patch` con 2 ficheros anotados y `ls` fallido con firma; forma del payload registrada sin texto | idem | La instalación real de José (`~/.codex/hooks.json` → `~/rompelo`) no cambia hasta el merge: NO VERIFICADO ahí. El `Stop` de Codex en el repo desechable no se cruzó (no estaba alistado) | pendiente |
