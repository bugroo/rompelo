# Relevo · rompelo · 05-09-2026 (noche)

Para quien retome esto, sea Claude Code, Codex o José. Todo lo escrito aquí está medido; lo
que no, lleva NO VERIFICADO.

## Dónde está

- Repo público: **github.com/bugroo/rompelo** (MIT). Clon local: `~/rompelo` (`main`, limpio,
  último commit `c0deb82` + este relevo). CI en verde: batería principal, batería de
  observación, check triestado y `verify --ci` sobre el propio repo.
- Contrato del repo: `.rompelo/task.json` (`ROMPELO-CODEX-01`, cerrado). Cualquier cambio lo
  reabre: `rompelo check` → `rompelo cruce --id rompelo.cruce-settings-claude` → `rompelo close`.
- Hooks conectados en `~/.claude/settings.json`: `Stop` → `adapters/claude/rompelo-stop.sh`,
  `PostToolUse` → `adapters/claude/rompelo-observe.sh`. Copia previa en
  `~/.claude/settings.json.bak-assure-2026-09-04`.
- Local, sin versionar: `checks/registry.local.json` (checks `claveon.*`), `config/repos.json`
  (allowlist: solo `~/rompelo`), `state/` (libros de sesión, nivel por repo, permisos).
- Shim de compatibilidad `~/assure/bin/assure` → `rompelo`, porque el hook de Codex aún apunta
  al nombre viejo.
- Memoria: `~/.claude/projects/-Users-rootml/memory/project_rompelo_stop_gate.md`. Plan original:
  `~/.claude/plans/dictamen-no-necesitas-fizzy-bear.md`. Diseño de la observación:
  `docs/observacion.md` (§12 dice qué está hecho y qué no).

## Qué hay (verificado)

| Pieza | Estado | Prueba |
|---|---|---|
| Gate Stop por contrato (checks sobre huella de contenido, junta, hallazgos, afirmaciones, scope, prueba en diff, salida mínima) | hecho, cruzado en vivo en Claude Code | `tests/rompelo-stop-test.sh` 77/77 |
| Registro de checks con argv sin shell, allowlist de repos, fail-closed, tope 3 bloqueos | hecho | ídem, con canario de inyección |
| `verify --ci` (juez independiente), `solo_local`, registro de respaldo en el repo | hecho | 6 casos + CI del propio repo |
| `init` con detección de checks, `close` con informe en llano | hecho | 4 casos, vistos en rojo por mutación |
| Capa de observación: libro, firma, perfiles D1, patrones D2, niveles, aviso, permiso | hecho, cruzado en vivo (por accidente, ver §12 del diseño) | `tests/rompelo-observe-test.sh` 30/30 |
| Corpus de 30 incidentes clasificados (I=10, S=6, C=4, M=3, T=3, A=2, R=2) | hecho | `python3 bin/rompelo-corpus.py` |
| README es/en con 4 imágenes (fuentes HTML en `docs/img/src`) | hecho | vistas en pantalla |

## Qué falta, en orden

**De José (nadie más puede):**
1. `~/.codex/hooks.json`: cambiar la ruta a `"$HOME/rompelo/bin/rompelo" hook codex` (y añadir
   la entrada `PostToolUse` de `adapters/codex/hooks.json`), luego confiar en `/hooks`.
2. Pegar a Codex `adapters/codex/PROMPT-CODEX.md` (`pbcopy <`): cruce en vivo desde Codex y el
   módulo de control positivo por check. Codex ya entregó dos hallazgos (arreglados) y no pudo
   escribir en `~/.codex` por su sandbox: lanzarlo con permiso de escritura.

**De Claude Code, con un «go»:**
3. Control negativo con sesiones reales: reproducir el libro de 5 sesiones sanas desde los
   transcripts de `~/.claude/projects/` (solo programa, códigos y rutas) y exigir cero alarmas.
   Sin esto el observador solo tiene control negativo sintético.
4. Informe de cierre con patrones y permisos (hoy no los lista).
5. Nivel 3: checks externos registrados (Stryker, semgrep) que el observador propone con permiso.
6. Mensajes del gate en inglés; etiqueta `v0.1.0`; CHANGELOG; CONTRIBUTING.
7. Anotar en cada incidente del corpus el disparador que lo habría visto (`docs/observacion.md` §9.3).

**Uso real antes que más construcción (recomendado):** `rompelo init` en una rama de
`ClaveON_B2C` con `claveon.test` y `claveon.humo-navegador`, y trabajar una tarea entera con
el gate puesto. Un uso real vale más que el punto 5.

## Avisos operativos

- En repos alistados, comandos con `auth`, `deploy`, `.env`, `pnpm add`, rutas `functions/api/`…
  suben el nivel a 2 y el gate exige `segunda_pasada` (y cruce/afirmaciones según perfil). Para
  bajarlo con rastro: `rompelo nivel bajar --motivo '…'`.
- La huella ignora `.rompelo/` y HEAD: commitear el mismo contenido no reabre; cambiar el
  contrato sí (hash del contrato en `cerrada.json`).
- La evidencia no está firmada: el gate frena el descuido, no la trampa. El juez es CI.
- Commits sin atribución a IA (regla de la casa; el hook `werixo-no-ai-attribution.sh` lo bloquea).

## NO VERIFICADO

- Codex en vivo (bloqueo y silencio desde una sesión de Codex).
- `clobs` con el gate (usa el mismo `settings.json`, no cruzado).
- Cómo renderiza GitHub el README en modo oscuro.
- Que `tool_response` de Claude Code traiga siempre `exit_code`; el observador cae a heurísticas si no.
