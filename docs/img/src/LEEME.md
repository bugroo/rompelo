# Fuentes de las imágenes

HTML estático, sin dependencias. Se renderizan a PNG a 1200 px de ancho y escala 2 con cualquier navegador headless (Playwright, Chrome --headless --screenshot). Los ficheros `en-*.html` son la versión inglesa.
